<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.selectjun.todo.mapper.UserAuthorityMapper">

    <insert id="save" parameterType="com.selectjun.todo.domain.UserAuthorityEntity">
        INSERT INTO USER_AUTHORITY (
            ID
            , AUTHORITY
        ) values (
            #{id}
            , "ROLE_USER"
        );
    </insert>

</mapper>